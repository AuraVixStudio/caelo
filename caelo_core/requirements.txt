# caelo-core (backend-sidecar) — zależności (bezpośrednie, luźne dolne granice).
# Instalacja do izolowanego venv:  python -m venv .venv && .venv\Scripts\pip install -r requirements.txt
# Powtarzalny build (PRZYPIĘTE wersje, P3-6):  pip install -r requirements.lock
fastapi>=0.110
uvicorn[standard]>=0.29
requests>=2.31
pillow>=10.0
python-dotenv>=1.0
# Silnik regex z per-search timeout= dla agentowego grep (ochrona ReDoS, P0-3).
regex>=2023.0.0
# WebSocket client do mostu realtime voice (zwykle ciągnięte przez uvicorn[standard],
# deklarujemy jawnie dla pewności).
websockets>=12.0
# Terminal (WS /terminal) — opcjonalne; bez tego agentowy run_command nadal działa.
pywinpty>=2.0 ; sys_platform == "win32"
